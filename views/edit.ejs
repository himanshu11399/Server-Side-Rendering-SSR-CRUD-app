<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white flex justify-center items-center min-h-screen">

    <div class="w-full max-w-md bg-zinc-700 p-6 rounded-lg shadow-lg">
        <h1 class="text-2xl mb-4">Edit User</h1>
        <form action="/edit/<%= user.email %>" method="POST" class="flex flex-col gap-4">
            <div>
                <label class="block mb-1" for="name">Name</label>
                <input type="text" id="name" name="name" value="<%= user.name %>"
                    class="w-full p-2 rounded text-black">
            </div>

            <div>
                <label class="block mb-1" for="email">Email</label>
                <input type="email" id="email" name="email" value="<%= user.email %>"
                    class="w-full p-2 rounded text-black" readonly>
            </div>

            <div>
                <label class="block mb-1" for="image">Image URL</label>
                <input type="text" id="image" name="image" value="<%= user.image %>"
                    class="w-full p-2 rounded text-black">
            </div>

            <div>
                <label class="block mb-1" for="password">Password</label>
                <input type="password" id="password" name="password" value="<%= user.password %>"
                    class="w-full p-2 rounded text-black">
            </div>

            <button type="submit"
                class="bg-green-500 hover:bg-green-600 p-2 rounded mt-2 text-black font-bold">Update User</button>
        </form>
        <div class="mt-4">
            <a href="/read" class="text-blue-400">Back to Users List</a>
        </div>
    </div>

</body>

</html>
